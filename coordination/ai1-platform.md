# ai1 — platform

**Owns:** the `platform` domain — dashboard, ServiceM8 integration, real-time webhooks,
dashboard cache, auth/sessions, feature gating — **plus** the shared files everyone
depends on (`servicem8.ts`, `accounts.ts`, `db.ts`, `storage.ts`). See [../CLAUDE.md](../CLAUDE.md).

## Log (newest first)

<!-- AI1: append here, newest at top. Start with a STATE dump (see ../coordination/README.md). -->
