# ai3 — customer-portal + stripe-billing

**Owns:** the `customer-portal` domain (email/magic-link auth, site selection, job views,
new job forms) **and** the `stripe-billing` domain (subscriptions, trials, plans,
checkout, billing webhooks). See [../CLAUDE.md](../CLAUDE.md).

## Log (newest first)

<!-- AI3: append here, newest at top. Start with a STATE dump (see ../coordination/README.md). -->
